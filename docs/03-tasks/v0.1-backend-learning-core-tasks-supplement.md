# Project Supplement: v0.1 Backend Learning Core Tasks

**Supplements**: `docs/03-tasks/v0.1-backend-learning-core-tasks.md`  
**This file is not skill-native output.**

## 1. Repository Completion Checklist

- [ ] Spec decisions are reflected in implementation。
- [ ] Draw.io diagram exists。
- [ ] Manifest exists。
- [ ] Runbook exists。
- [ ] H2 tests pass。
- [ ] Local run succeeds。
- [ ] Swagger UI reachable。
- [ ] OpenAPI JSON reachable。
- [ ] Feynman reflection captured。
- [ ] Review/security findings resolved or recorded。

## 2. First TDD Slice Recommendation

Start with the smallest green path:

```text
Common API envelope
  -> Topic create failing test
  -> Topic create implementation
  -> H2 test pass
```

Then expand:

```text
Topic full CRUD
  -> Topic delete guard
  -> Experiment CRUD
  -> Experiment status transition
```

## 3. Required Runbook Path

```text
docs/04-runbooks/v0.1-backend-learning-core-runbook.md
```

## 4. Required Verification Commands

```bash
cd backend
mvn test -Dspring.profiles.active=test
```

```bash
cd backend
mvn spring-boot:run -Dspring-boot.run.profiles=local
```

## 5. Claude Review Handoff

For non-trivial backend implementation, use Claude Code as independent reviewer with this prompt pointer:

```text
Read AGENTS.md, the active spec, architecture/design/data-model/API guide, task list, task supplement, manifest, and runbook. Review the diff for spec alignment, correctness, validation, SQL safety, error disclosure, and verification evidence.
```
