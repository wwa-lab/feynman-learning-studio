# Project Supplement: v0.1 Backend Learning Core Architecture

**Supplements**: `docs/02-design/v0.1-backend-learning-core-architecture.md`  
**Reason**: 补充本 repo 的 SDD gate、目录映射、grounding evidence 和实现前决策。  
**This file is not skill-native output.**

## 1. Grounding Pass

当前仓库没有 backend implementation：

- 未发现 `backend/`。
- 未发现 `pom.xml`。
- 未发现 Spring Boot application class。
- 未发现 controller、service、mapper、domain、DTO。

因此 architecture 文档描述的是目标架构，不是已有实现。

## 2. Repository Mapping

Skill native output 通常使用泛化文件名。本 repo 使用 slice-specific 文件名：

| Skill Artifact | Repo Path |
|---|---|
| architecture.md | `docs/02-design/v0.1-backend-learning-core-architecture.md` |
| architecture supplement | `docs/02-design/v0.1-backend-learning-core-architecture-supplement.md` |
| draw.io diagram | `docs/02-design/v0.1-backend-learning-core.drawio` |

## 3. Decisions Applied

- `DELETE /api/topics/{id}`: topic 被 experiment 引用时拒绝删除。
- Error code: `TOPIC_IN_USE`。
- v0.1 delete strategy: physical delete。
- Swagger UI: `http://localhost:8080/swagger-ui/index.html`。
- OpenAPI JSON: `http://localhost:8080/v3/api-docs`。
- Future integration database: PostgreSQL。

## 4. Additional Project Gates

- Architecture 必须和 `docs/00-project-rules/backend-standards.md` 的 package-by-feature 规则一致。
- Architecture 不得引入 frontend、auth、member/team、RunEvidence model、Redis/MQ/Nacos/Gateway。
- 进入 implementation 前必须存在 draw.io diagram。

## 5. Next Gate

Architecture gate 通过后，进入 `architecture-to-design` skill native output，并在 design supplement 中补充 repo-specific implementation handoff。
